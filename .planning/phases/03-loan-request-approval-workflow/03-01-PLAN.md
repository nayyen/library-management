---
phase: 03-loan-request-approval-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/peminjaman.py
  - backend/app/routers/peminjaman.py
  - backend/app/main.py
  - backend/tests/test_peminjaman.py
autonomous: true
requirements: [LOAN-01, LOAN-02, LOAN-03, LOAN-04, LOAN-05, LOAN-06]

must_haves:
  truths:
    - "A mahasiswa can POST a loan request for a tersedia copy, creating a peminjaman row with status_peminjaman=menunggu_persetujuan and flipping the salinan to dipesan (D-01, LOAN-01)"
    - "A mahasiswa with 5 active loans (menunggu_persetujuan/siap_diambil/dipinjam) gets HTTP 400 on /ajukan (D-04, LOAN-02)"
    - "A mahasiswa with is_diblokir=TRUE gets HTTP 400 on /ajukan (D-04, LOAN-03)"
    - "The mahasiswa GET /api/peminjaman response carries the caller's own is_diblokir flag (top-level on PeminjamanListOut), so the frontend can render the LOAN-03 blocked banner from real data (D-04, LOAN-03)"
    - "A pustakawan can approve (->siap_diambil, sets tanggal_siap_ambil) or reject (->ditolak, resets salinan to tersedia) a pending request via /persetujuan (D-07, LOAN-04)"
    - "A siap_diambil row whose tanggal_siap_ambil + 2 days < now auto-becomes dibatalkan and resets its salinan to tersedia on the next /peminjaman GET (D-12, LOAN-05)"
    - "A pustakawan can mark a siap_diambil loan handed over via /serahkan, moving it to dipinjam, setting salinan to dipinjam, and setting tanggal_tenggat to 14 days from now (LOAN-06)"
    - "GET /api/peminjaman without a token returns 401 (AUTH-04 carried forward)"
  artifacts:
    - path: "backend/app/schemas/peminjaman.py"
      provides: "Request/response Pydantic models for loan endpoints"
      contains: "class PeminjamanAjukan"
    - path: "backend/app/routers/peminjaman.py"
      provides: "Loan request/approval/handover/list endpoints + lazy-sweep helper"
      contains: "router = APIRouter(prefix=\"/api/peminjaman\""
    - path: "backend/tests/test_peminjaman.py"
      provides: "RED->GREEN tests for all 6 LOAN requirements"
      contains: "def test_ajukan"
    - path: "backend/app/main.py"
      provides: "peminjaman router registration"
      contains: "peminjaman"
  key_links:
    - from: "backend/app/main.py"
      to: "app.routers.peminjaman.router"
      via: "app.include_router"
      pattern: "include_router\\(peminjaman.router\\)"
    - from: "backend/app/routers/peminjaman.py"
      to: "Peminjaman / SalinanBuku models"
      via: "transactional status updates"
      pattern: "StatusPeminjaman\\.|StatusSalinan\\."
    - from: "backend/app/routers/peminjaman.py mahasiswa GET branch"
      to: "PeminjamanListOut.is_diblokir"
      via: "reads user.is_diblokir into the response"
      pattern: "is_diblokir"
---

<objective>
Build the complete `/api/peminjaman` backend slice: schemas, router (request → approval/rejection → handover → shared list with lazy pickup-window sweep), router registration, and a RED→GREEN test suite covering all six LOAN requirements. This is the API+DB foundation that the two frontend slices (Plans 02 and 03) consume.

Purpose: Without this API there is no loan lifecycle — it is the engine for the whole phase. It owns server-side enforcement of the 5-loan limit (LOAN-02) and block check (LOAN-03), the approval/rejection state machine with salinan sync (LOAN-04), the 2x24h lazy auto-cancellation (LOAN-05, D-12), and the 14-day handover (LOAN-06).
Output: `backend/app/schemas/peminjaman.py`, `backend/app/routers/peminjaman.py`, updated `backend/app/main.py`, and `backend/tests/test_peminjaman.py` (all tests GREEN).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-loan-request-approval-workflow/03-CONTEXT.md
@.planning/phases/03-loan-request-approval-workflow/03-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols this plan creates (exclude from drift/source-grounding checks):
- File `backend/app/schemas/peminjaman.py` with classes: `PeminjamanAjukan`, `PeminjamanItemOut`, `PeminjamanListOut`, `PustakawanAntrianOut`, `PersetujuanAction` (enum/literal for `setujui`/`tolak`), `PersetujuanBody`.
- File `backend/app/routers/peminjaman.py` with: `router` (APIRouter prefix `/api/peminjaman`), helpers `_pustakawan_only`, `_mahasiswa_only`, `_sweep_expired_pickups`, `_active_loan_count`, and endpoints `ajukan_peminjaman`, `daftar_peminjaman`, `proses_persetujuan`, `serahkan_peminjaman`.
- File `backend/tests/test_peminjaman.py` with test functions prefixed `test_ajukan*`, `test_persetujuan*`, `test_serahkan*`, `test_list_peminjaman*`, `test_sweep*`.
- Endpoints: `POST /api/peminjaman/ajukan`, `GET /api/peminjaman`, `PUT /api/peminjaman/{id}/persetujuan`, `PUT /api/peminjaman/{id}/serahkan`.
- ACTIVE_STATUSES constant (the set counted for LOAN-02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define peminjaman schemas + write RED test suite for all 6 LOAN requirements</name>
  <files>backend/app/schemas/peminjaman.py, backend/tests/test_peminjaman.py</files>
  <read_first>
    - backend/app/schemas/buku.py (BaseModel + Field validator + from_attributes + list-wrapper + inheritance patterns to mirror)
    - backend/tests/test_buku.py (test structure: `_register_and_login` mahasiswa helper lines 19-37, `_create_pustakawan_token` helper lines 290-313, `_auth_header`, direct model seeding with db_session, 401/403/409 assertions)
    - backend/tests/conftest.py (SQLite in-memory fixtures: `client`, `db_session`, autouse `setup_database`)
    - backend/app/models/peminjaman.py (columns: id, id_pengguna, id_salinan_buku, tanggal_pengajuan default-now, tanggal_siap_ambil, tanggal_pinjam, tanggal_tenggat, tanggal_kembali, status_peminjaman default menunggu_persetujuan, total_denda)
    - backend/app/models/enums.py (StatusPeminjaman: menunggu_persetujuan/siap_diambil/dipinjam/dibatalkan/dikembalikan/ditolak; StatusSalinan: tersedia/dipesan/dipinjam; PeranPengguna)
    - backend/app/models/salinan_buku.py and backend/app/models/pengguna.py (FK targets, is_diblokir flag)
  </read_first>
  <action>
    Create `backend/app/schemas/peminjaman.py` mirroring `schemas/buku.py` conventions (Pydantic v2 BaseModel, `model_config = {"from_attributes": True}` where reading ORM rows, `Field(...)` for required inputs). Define:
    - `PeminjamanAjukan`: single required field `id_salinan_buku: str`.
    - `PeminjamanItemOut`: `id: str`, `status_peminjaman: str`, `judul: str`, `penulis: str`, `lokasi_rak: str`, `nama_mahasiswa: str`, `tanggal_pengajuan: datetime`, `tanggal_siap_ambil: datetime | None`, `tanggal_tenggat: datetime | None` (datetime fields serialized as ISO strings by FastAPI). This single item shape serves both roles.
    - `PeminjamanListOut`: `items: list[PeminjamanItemOut]`, `total: int`, AND `is_diblokir: bool` (the caller mahasiswa's own block flag, D-04/LOAN-03 — top-level on the mahasiswa "Pinjaman Saya" response so the frontend banner reads real data, not a client guess). This is the mahasiswa "Pinjaman Saya" sorted list, D-09.
    - `PustakawanAntrianOut`: `menunggu_persetujuan: list[PeminjamanItemOut]`, `siap_diambil: list[PeminjamanItemOut]` (D-06 stacked sections). Use ONE unified response model `PeminjamanResponse` with optional `items`, `total`, `is_diblokir`, `menunggu_persetujuan`, `siap_diambil` (all nullable) so the GET endpoint has a single `response_model`; the mahasiswa branch populates `items`/`total`/`is_diblokir`, the pustakawan branch populates `menunggu_persetujuan`/`siap_diambil`. Document the chosen shape in the SUMMARY, explicitly naming `is_diblokir` as a mahasiswa-branch field so Plans 02/03 consume it directly.
    - `PersetujuanBody`: `aksi: str` constrained to literal values `"setujui"` or `"tolak"` (use `Literal["setujui", "tolak"]`).
    Then write `backend/tests/test_peminjaman.py` (RED — endpoints do not exist yet) following `test_buku.py` structure. Copy the `_register_and_login` (mahasiswa) and `_create_pustakawan_token` helpers and `_auth_header`. Seed Buku + SalinanBuku + Pengguna directly via `db_session` as in test_buku. Tests REQUIRED:
    - `test_ajukan_peminjaman_mahasiswa`: POST /api/peminjaman/ajukan with a tersedia salinan id → 201, response status_peminjaman=="menunggu_persetujuan"; re-query DB: salinan.status_ketersediaan=="dipesan" (LOAN-01, D-01/D-02).
    - `test_ajukan_forbidden_for_pustakawan`: pustakawan token → 403.
    - `test_ajukan_unauthorized`: no token → 401.
    - `test_ajukan_rejected_when_blocked`: seed mahasiswa with is_diblokir=True → 400 (LOAN-03).
    - `test_ajukan_rejected_at_loan_limit`: seed 5 peminjaman rows for the mahasiswa with statuses in {menunggu_persetujuan, siap_diambil, dipinjam} → 400 (LOAN-02). A 6th non-active (ditolak) row must NOT count.
    - `test_ajukan_rejected_when_salinan_not_tersedia`: salinan already dipesan/dipinjam → 409.
    - `test_persetujuan_setujui`: pustakawan PUT /api/peminjaman/{id}/persetujuan {"aksi":"setujui"} on a menunggu_persetujuan row → 200, status==siap_diambil, tanggal_siap_ambil set non-null (LOAN-04).
    - `test_persetujuan_tolak`: {"aksi":"tolak"} → 200, status==ditolak, linked salinan reset to tersedia (LOAN-04).
    - `test_persetujuan_forbidden_for_mahasiswa`: mahasiswa token → 403.
    - `test_persetujuan_404`: nonexistent id → 404.
    - `test_persetujuan_invalid_state`: row already dipinjam → 409 (cannot approve a non-pending row).
    - `test_serahkan`: pustakawan PUT /api/peminjaman/{id}/serahkan on a siap_diambil row → 200, status==dipinjam, salinan==dipinjam, tanggal_tenggat ≈ now + 14 days (assert it is set and roughly 14 days ahead, e.g. 13 < (tanggal_tenggat - now).days <= 14) (LOAN-06).
    - `test_serahkan_invalid_state`: row in menunggu_persetujuan → 409.
    - `test_list_peminjaman_mahasiswa`: returns only the caller's rows sorted most-recent-first AND a top-level `is_diblokir` field reflecting the caller's flag (assert `is_diblokir in response.json()` and equals the seeded value) (D-09, D-04/LOAN-03).
    - `test_list_peminjaman_mahasiswa_blocked_flag`: seed the caller mahasiswa with is_diblokir=True → GET /api/peminjaman returns `is_diblokir == True` at the top level (proves the LOAN-03 banner has a real data source).
    - `test_list_peminjaman_unauthorized`: no token → 401.
    - `test_sweep_expired_pickup`: seed a siap_diambil row with tanggal_siap_ambil = now - 3 days, then GET /api/peminjaman → that row's status is dibatalkan and its salinan reset to tersedia (LOAN-05, D-12). Use a tanggal_siap_ambil older than 2 days to trigger the sweep.
    Do NOT place fenced code in the plan; write real Python in the files. Run pytest to confirm RED (import/404 failures expected).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_peminjaman.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/peminjaman.py` contains `class PeminjamanAjukan` and `class PeminjamanItemOut`
    - `backend/app/schemas/peminjaman.py` contains `is_diblokir` on the mahasiswa-facing response model (PeminjamanListOut / unified PeminjamanResponse)
    - `backend/tests/test_peminjaman.py` contains `def test_ajukan_rejected_at_loan_limit`, `def test_ajukan_rejected_when_blocked`, `def test_sweep_expired_pickup`, `def test_serahkan`, `def test_persetujuan_tolak`, `def test_list_peminjaman_mahasiswa_blocked_flag`
    - `grep -v '^#' backend/tests/test_peminjaman.py | grep -c "def test_" ` returns at least 15
    - Running `python -m pytest tests/test_peminjaman.py` FAILS (RED) because router/endpoints do not yet exist (collection errors or 404/405 responses)
  </acceptance_criteria>
  <done>Schemas defined (mahasiswa response carries is_diblokir); full RED test suite exists covering LOAN-01..06 + 401/403 + the is_diblokir data source; pytest run confirms RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement peminjaman router + register it (turn tests GREEN)</name>
  <files>backend/app/routers/peminjaman.py, backend/app/main.py</files>
  <read_first>
    - backend/app/routers/buku.py (verbatim patterns: imports block lines 1-26, `_pustakawan_only` helper lines 139-145, create-with-precheck `tambah_buku` lines 148-190, 404-then-mutate `ubah_buku` lines 193-245, list-with-derived-data `daftar_buku` lines 29-82, inline HTTPException error shape)
    - backend/app/schemas/peminjaman.py (the schemas just created in Task 1 — import these)
    - backend/app/dependencies/auth.py (`get_current_user` returns Pengguna with `.peran`, `.id`, `.is_diblokir`)
    - backend/app/main.py (current registration block: `from app.routers import autentikasi, buku` then `app.include_router(buku.router)`)
    - backend/app/models/peminjaman.py, salinan_buku.py, pengguna.py, buku.py, enums.py (ORM fields for joins/updates)
  </read_first>
  <action>
    Create `backend/app/routers/peminjaman.py` with `router = APIRouter(prefix="/api/peminjaman", tags=["peminjaman"])`. Import `Peminjaman`, `SalinanBuku`, `Buku`, `Pengguna`, `StatusSalinan`, `StatusPeminjaman`, `PeranPengguna`, and the Task-1 schemas. Define module constant `ACTIVE_STATUSES = [StatusPeminjaman.menunggu_persetujuan, StatusPeminjaman.siap_diambil, StatusPeminjaman.dipinjam]` (Claude's-discretion active-loan definition per CONTEXT.md). Implement:
    - `_pustakawan_only(user)`: copy verbatim from buku.py (403 "Akses ditolak.").
    - `_mahasiswa_only(user)`: 403 if `user.peran != PeranPengguna.mahasiswa`, detail "Akses ditolak.".
    - `_active_loan_count(db, id_pengguna)`: count Peminjaman where id_pengguna matches and status_peminjaman in ACTIVE_STATUSES.
    - `_sweep_expired_pickups(db)`: query all Peminjaman with status_peminjaman==siap_diambil and `tanggal_siap_ambil + timedelta(days=2) < datetime.now(timezone.utc)`; for each, set status_peminjaman=dibatalkan and reset the linked SalinanBuku.status_ketersediaan=tersedia; commit once. Compare timezone-aware datetimes (use `datetime.now(timezone.utc)`).
    - `POST /ajukan` (`ajukan_peminjaman`, response 201, body `PeminjamanAjukan`): `_mahasiswa_only(user)`; if `user.is_diblokir` → 400 "Akun Anda diblokir karena denda belum lunas." (LOAN-03); if `_active_loan_count(db, user.id) >= 5` → 400 "Anda sudah memiliki 5 pinjaman aktif." (LOAN-02); fetch salinan by id, 404 if missing; if `salinan.status_ketersediaan != StatusSalinan.tersedia` → 409 "Salinan ini sudah tidak tersedia."; create Peminjaman(id_pengguna=user.id, id_salinan_buku=salinan.id, status_peminjaman=menunggu_persetujuan), set salinan.status_ketersediaan=dipesan, commit both in one transaction, refresh, return the row.
    - `GET ""` (`daftar_peminjaman`, response_model the unified `PeminjamanResponse`): call `_sweep_expired_pickups(db)` FIRST (once, before role branch). Then branch on `user.peran`: pustakawan → two lists (menunggu_persetujuan, siap_diambil) joined to SalinanBuku→Buku and Pengguna to fill judul/penulis/lokasi_rak/nama_mahasiswa; mahasiswa → all rows where id_pengguna==user.id ordered by tanggal_pengajuan desc, returned as `items`+`total` AND `is_diblokir=user.is_diblokir` (read straight off the authenticated Pengguna — this is the LOAN-03 banner's authoritative data source per the must_have truth). Build join-then-construct-items like `daftar_buku`.
    - `PUT /{id_peminjaman}/persetujuan` (`proses_persetujuan`, body `PersetujuanBody`): `_pustakawan_only`; fetch by id, 404 if missing; if status_peminjaman != menunggu_persetujuan → 409 "Pengajuan tidak dalam status menunggu persetujuan."; if aksi=="setujui": status=siap_diambil, tanggal_siap_ambil=now(utc) (salinan stays dipesan); if aksi=="tolak": status=ditolak, reset salinan to tersedia; commit, return row.
    - `PUT /{id_peminjaman}/serahkan` (`serahkan_peminjaman`): `_pustakawan_only`; fetch by id, 404 if missing; if status != siap_diambil → 409 "Pinjaman tidak dalam status siap diambil."; set status=dipinjam, salinan.status_ketersediaan=dipinjam, tanggal_pinjam=now(utc), tanggal_tenggat=now(utc)+timedelta(days=14) (LOAN-06); commit, return row.
    Use inline `HTTPException(status_code=..., detail="...")` for every error (no central handler), matching buku.py. Then edit `backend/app/main.py`: change import to `from app.routers import autentikasi, buku, peminjaman` and add `app.include_router(peminjaman.router)` after the buku registration.
    Run pytest until all test_peminjaman tests pass, then run the full backend suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/peminjaman.py` contains `router = APIRouter(prefix="/api/peminjaman"` and `def _sweep_expired_pickups(`
    - `backend/app/routers/peminjaman.py` mahasiswa GET branch sets `is_diblokir` from `user.is_diblokir` (grep `is_diblokir` returns a hit in the router)
    - `backend/app/main.py` contains `peminjaman` in the routers import line and `app.include_router(peminjaman.router)`
    - `cd backend && python -m pytest tests/test_peminjaman.py` exits 0 (all GREEN)
    - `cd backend && python -m pytest tests/` exits 0 (full suite GREEN, no Phase 1/2 regressions)
    - POST /api/peminjaman/ajukan as mahasiswa for a tersedia copy returns 201 and the linked salinan becomes dipesan in the DB
    - GET /api/peminjaman with a siap_diambil row older than 2x24h returns that row as dibatalkan (LOAN-05 sweep)
    - GET /api/peminjaman as a blocked mahasiswa returns top-level `is_diblokir: true`
  </acceptance_criteria>
  <done>All 6 LOAN requirements enforced server-side; mahasiswa GET exposes is_diblokir; test_peminjaman GREEN; full suite GREEN; router registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`/api/peminjaman/*`) | Authenticated but untrusted JSON crosses here; role and business-rule gates enforced server-side |
| API → DB | Status transitions and salinan sync must be transactional to avoid orphaned/inconsistent states |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | `PUT /persetujuan`, `PUT /serahkan` | mitigate | `_pustakawan_only(user)` gate (403) on both endpoints — copied verbatim from buku.py role-gate; test_persetujuan_forbidden_for_mahasiswa asserts 403 |
| T-03-02 | Elevation of Privilege | `POST /ajukan` | mitigate | `_mahasiswa_only(user)` gate (403); test_ajukan_forbidden_for_pustakawan asserts 403 |
| T-03-03 | Spoofing | all endpoints | mitigate | `get_current_user` JWT validation (401 on missing/expired/invalid); test_ajukan_unauthorized + test_list_peminjaman_unauthorized assert 401 |
| T-03-04 | Tampering | `POST /ajukan` business gates | mitigate | Server-side LOAN-02 (active-count>=5 → 400) and LOAN-03 (is_diblokir → 400) enforced regardless of client-side D-04 pre-check/disable; tests assert 400 |
| T-03-05 | Tampering | salinan double-booking (race) | mitigate | `/ajukan` rechecks `salinan.status_ketersediaan == tersedia` and returns 409 if not; test_ajukan_rejected_when_salinan_not_tersedia asserts 409 |
| T-03-06 | Tampering | invalid state transitions | mitigate | persetujuan requires current==menunggu_persetujuan (409 otherwise); serahkan requires current==siap_diambil (409 otherwise); both covered by *_invalid_state tests |
| T-03-07 | Information Disclosure | mahasiswa loan list | mitigate | mahasiswa GET branch filters `id_pengguna == user.id` — a mahasiswa cannot read other users' loans; the top-level `is_diblokir` returned is the caller's own flag, not another user's; test_list_peminjaman_mahasiswa asserts only own rows returned |
| T-03-SC | Tampering | npm/pip installs | accept | No new packages introduced — peminjaman model + ENUM already migrated in Phase 1; only new router/schema/test Python files. No package-manager installs in this phase. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/` exits 0 (all Phase 1+2+3 tests GREEN)
- All 6 LOAN requirements have at least one passing test asserting the documented behavior
- 401 (no token), 403 (wrong role), 404 (missing row), 409 (invalid state / unavailable copy) all asserted
- LOAN-03 blocked-banner data source verified: GET /api/peminjaman returns top-level is_diblokir for the mahasiswa branch
- LOAN-05 lazy sweep verified by an expired-pickup test (no scheduled worker, per D-12)
</verification>

<success_criteria>
- `/api/peminjaman` router registered and serving ajukan/list/persetujuan/serahkan
- Loan limit (LOAN-02), block check (LOAN-03), approval/rejection with salinan sync (LOAN-04), lazy 2x24h cancellation (LOAN-05), 14-day handover (LOAN-06) all enforced and tested
- Mahasiswa GET response exposes is_diblokir so the frontend LOAN-03 banner renders from real data
- No regressions in Phase 1/2 test suite
</success_criteria>

<output>
Create `.planning/phases/03-loan-request-approval-workflow/03-01-SUMMARY.md` when done. Document the chosen unified GET response shape (explicitly listing the mahasiswa-branch `is_diblokir` field), the ACTIVE_STATUSES definition, and the endpoint signatures so Plans 02/03 can consume them without re-deriving.
</output>
</output>
