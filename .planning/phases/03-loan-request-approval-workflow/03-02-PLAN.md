---
phase: 03-loan-request-approval-workflow
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/components/StatusBadge.jsx
  - frontend/src/components/BlockedBanner.jsx
  - frontend/src/components/LoanRequestModal.jsx
  - frontend/src/components/SalinanTable.jsx
  - frontend/src/pages/BukuDetailPage.jsx
  - frontend/src/pages/PinjamanPage.jsx
  - frontend/src/router.jsx
autonomous: false
requirements: [LOAN-01, LOAN-02, LOAN-03]

must_haves:
  truths:
    - "router.jsx's /pinjaman route renders the new shared PinjamanPage instead of ComingSoonPage, with content branching on the authenticated user's peran — mahasiswa and pustakawan see different content on the same route (D-05)"
    - "A mahasiswa on /katalog/{id} sees a 'Pinjam' button on each tersedia copy row in SalinanTable, and clicking it opens the LoanRequestModal (D-01, D-02)"
    - "Submitting the LoanRequestModal POSTs /api/peminjaman/ajukan and on success shows a success Toast, closes the modal, and re-fetches so the copy flips from tersedia to dipesan (D-03, LOAN-01)"
    - "When the mahasiswa is at the 5-loan limit (active-loan count >= 5) a BlockedBanner variant='limit' shows at the top of /katalog/{id} and all Pinjam buttons are disabled (D-04, LOAN-02)"
    - "When the mahasiswa's is_diblokir is TRUE (read from the GET /api/peminjaman response's top-level is_diblokir field) a BlockedBanner variant='blocked' shows at the top of /katalog/{id} and all Pinjam buttons are disabled (D-04, LOAN-03)"
    - "A mahasiswa visiting /pinjaman sees a single 'Pinjaman Saya' list, sorted most-recent-first, with a StatusBadge per row for all 5 statuses (D-09, D-10)"
    - "For siap_diambil rows the list shows an absolute pickup deadline 'Ambil sebelum {tanggal_siap_ambil + 2 hari}' (D-11)"
    - "A LOAN-02/LOAN-03 400 from the API surfaces as the specific Indonesian error Toast and keeps the modal open for retry (D-03)"
  artifacts:
    - path: "frontend/src/components/StatusBadge.jsx"
      provides: "5-status peminjaman pill badge"
      contains: "menunggu_persetujuan"
    - path: "frontend/src/components/LoanRequestModal.jsx"
      provides: "loan request confirmation modal (D-03)"
      contains: "Ajukan Peminjaman"
    - path: "frontend/src/components/BlockedBanner.jsx"
      provides: "blocked/limit persistent banner (D-04)"
      contains: "Batas Pinjaman Tercapai"
    - path: "frontend/src/pages/PinjamanPage.jsx"
      provides: "shared /pinjaman page (mahasiswa branch in this plan)"
      contains: "Pinjaman Saya"
  key_links:
    - from: "frontend/src/components/LoanRequestModal.jsx"
      to: "/api/peminjaman/ajukan"
      via: "api.post on submit"
      pattern: "peminjaman/ajukan"
    - from: "frontend/src/pages/BukuDetailPage.jsx"
      to: "LoanRequestModal"
      via: "SalinanTable onPinjam opens modal"
      pattern: "LoanRequestModal"
    - from: "frontend/src/pages/BukuDetailPage.jsx"
      to: "GET /api/peminjaman is_diblokir"
      via: "pre-check reads top-level is_diblokir into the blocked banner variant"
      pattern: "is_diblokir"
    - from: "frontend/src/pages/PinjamanPage.jsx"
      to: "/api/peminjaman"
      via: "api.get in useEffect"
      pattern: "api.get\\('/peminjaman'"
    - from: "frontend/src/router.jsx"
      to: "PinjamanPage"
      via: "route element replaces ComingSoonPage"
      pattern: "<PinjamanPage"
---

<objective>
Deliver the mahasiswa loan-request vertical slice end-to-end (UI → API → DB): the "Pinjam" entry point on copy rows, the LoanRequestModal confirmation flow, the BlockedBanner pre-check (D-04), the StatusBadge component, and the mahasiswa "Pinjaman Saya" view on a real `/pinjaman` route. After this plan a mahasiswa can find a book, request a specific copy, see the result, and track all their requests.

Purpose: Closes LOAN-01 (request), LOAN-02 (limit), LOAN-03 (block) from the user's side — the core "request a loan" capability of the phase. Plan 01 already enforces these rules server-side and exposes the mahasiswa's own `is_diblokir` on the GET /api/peminjaman response; this plan wires the mahasiswa experience and reads that flag for the blocked banner.
Output: New `StatusBadge.jsx`, `BlockedBanner.jsx`, `LoanRequestModal.jsx`; modified `SalinanTable.jsx`, `BukuDetailPage.jsx`, new `PinjamanPage.jsx` (mahasiswa branch), `router.jsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-loan-request-approval-workflow/03-CONTEXT.md
@.planning/phases/03-loan-request-approval-workflow/03-PATTERNS.md
@.planning/phases/03-loan-request-approval-workflow/03-UI-SPEC.md
@.planning/phases/03-loan-request-approval-workflow/03-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols this plan creates (exclude from drift/source-grounding checks):
- `frontend/src/components/StatusBadge.jsx` — default export `StatusBadge({ status, compact })`.
- `frontend/src/components/BlockedBanner.jsx` — default export `BlockedBanner({ variant })` where variant in {`limit`, `blocked`, null}.
- `frontend/src/components/LoanRequestModal.jsx` — default export `LoanRequestModal({ buku, salinan, mahasiswaNama, onClose, onSubmitted, onError })`.
- `SalinanTable` new props: `peran`, `onPinjam`, `pinjamDisabled`.
- `frontend/src/pages/PinjamanPage.jsx` — default export `PinjamanPage` (mahasiswa branch implemented here; pustakawan branch is Plan 03).
- New route element `<PinjamanPage />` at path `pinjaman` (replaces ComingSoonPage).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build StatusBadge + BlockedBanner + LoanRequestModal components</name>
  <files>frontend/src/components/StatusBadge.jsx, frontend/src/components/BlockedBanner.jsx, frontend/src/components/LoanRequestModal.jsx</files>
  <read_first>
    - frontend/src/components/AvailabilityBadge.jsx (variant-map + `compact` prop + `inline-flex items-center gap-1 border rounded-full` shell to port to StatusBadge)
    - frontend/src/components/EmptyState.jsx (icon+heading+body block layout to adapt for BlockedBanner)
    - frontend/src/components/BookFormModal.jsx (modal shell, backdrop/Escape close, header/footer, submit handler with try/catch + spinner button — the LoanRequestModal analog)
    - frontend/src/components/BookCoverPlaceholder.jsx (small cover for the "Buku Terpilih" block)
    - frontend/src/lib/api.js (the api client used for api.post)
    - .planning/phases/03-loan-request-approval-workflow/03-UI-SPEC.md (Status Badge Color Map; Copywriting Contract for modal text; LoanRequestModal layout section: header/3-body-sections/footer, max-w-lg, focus management)
  </read_first>
  <action>
    Build three hand-built React + Tailwind components (no shadcn — carried forward per UI-SPEC).
    1. `StatusBadge.jsx`: port `AvailabilityBadge`'s variant-map + `compact` pattern. Props `{ status, compact = false }`. Map the 5 statuses to the exact styles from the UI-SPEC Status Badge Color Map: menunggu_persetujuan → label "Menunggu Persetujuan", `bg-antique-gold/10 text-antique-gold border-antique-gold/20`, icon `hourglass_empty`; siap_diambil → "Siap Diambil", `bg-sage-green/10 text-sage-green border-sage-green/20`, `event_available`; dipinjam → "Dipinjam", `bg-primary-container/10 text-primary-container border-primary-fixed`, `inventory_2`; ditolak → "Ditolak", `bg-alert-crimson/10 text-alert-crimson border-alert-crimson/20`, `cancel`; dibatalkan → "Dibatalkan", `bg-surface-container text-on-surface-variant border-outline-variant`, `close`. Keep the `inline-flex items-center gap-1 border rounded-full font-label-sm` shell; compact = `px-2 py-0.5 text-[11px]`, default = `px-3 py-1 text-label-sm`. Icon `aria-hidden="true"`.
    2. `BlockedBanner.jsx`: props `{ variant }` where variant ∈ {`limit`, `blocked`, null}; render nothing if falsy. Two variants with exact copy from UI-SPEC Copywriting Contract: `limit` → heading "Batas Pinjaman Tercapai", body "Anda memiliki 5 pinjaman aktif, batas maksimum yang diizinkan. Selesaikan atau kembalikan salah satu pinjaman sebelum mengajukan yang baru.", icon `inventory_2`, `bg-antique-gold/10 border-antique-gold/30 text-on-surface`; `blocked` → heading "Akun Diblokir", body "Akun Anda diblokir karena ada denda yang belum dibayar. Selesaikan pembayaran denda di perpustakaan untuk mengajukan pinjaman baru.", icon `block`, `bg-alert-crimson/10 border-alert-crimson/30 text-alert-crimson`. Outer `role="status" aria-live="polite" rounded-lg border p-4 flex items-start gap-3 mb-6`. Heading `label-md`, body `body-lg`.
    3. `LoanRequestModal.jsx`: props `{ buku, salinan, mahasiswaNama, onClose, onSubmitted, onError }`. Modal shell from BookFormModal (backdrop `fixed inset-0 z-50 ... bg-primary/40 backdrop-blur-sm p-4`, `role="dialog" aria-modal="true" aria-labelledby`, Escape + backdrop close), widened to `max-w-lg`, body `space-y-8` with the 3 sections per UI-SPEC: (1) "Buku Terpilih" — small BookCoverPlaceholder + `border-l-4 border-antique-gold`, book judul (`headline-sm`), penulis, "Lokasi Rak: {salinan.lokasi_rak}"; section label "Buku Terpilih" uppercase `text-antique-gold`. (2) "Informasi Peminjam" — read-only display blocks: "Nama Lengkap" = mahasiswaNama, "Peran" = "Mahasiswa". (3) "Ringkasan Peminjaman" — info block (`bg-primary-fixed/30 border-primary-fixed-dim`, `info` icon `text-ink-blue`), body copy exactly "Peminjaman berlaku selama 14 hari setelah buku diserahkan oleh pustakawan. Pastikan mengembalikan buku tepat waktu untuk menghindari denda sebesar Rp 1.000/hari.", an "Estimasi Tenggat Waktu" row showing today + 14 days formatted "D MMMM YYYY" in Indonesian, and footnote "*Tanggal pasti akan ditentukan saat buku diserahkan oleh pustakawan." Footer: "Batal" (Ghost) + "Ajukan Peminjaman" (`bg-antique-gold text-on-primary`, `send` icon, spinner + disabled while in flight). Submit handler: `api.post('/peminjaman/ajukan', { id_salinan_buku: salinan.id })`; on success call `onSubmitted(res.data)` then `onClose()`; on error read `err.response?.data?.detail`, map to specific Toast copy (LOAN-02 / LOAN-03 / "Salinan ini sudah tidak tersedia. Pilih salinan lain." / generic "Gagal mengirim pengajuan. Silakan coba lagi.") and call `onError(message)` — KEEP MODAL OPEN on error (do not call onClose). Date formatting: implement an inline helper using `toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' })`. Min 44px touch targets on footer + close buttons; focus moves into modal on open, returns to trigger on close.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/components/StatusBadge.jsx src/components/BlockedBanner.jsx src/components/LoanRequestModal.jsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/StatusBadge.jsx` contains all 5 status keys: `menunggu_persetujuan`, `siap_diambil`, `dipinjam`, `ditolak`, `dibatalkan`
    - `frontend/src/components/BlockedBanner.jsx` contains "Batas Pinjaman Tercapai" and "Akun Diblokir" and `role="status"`
    - `frontend/src/components/LoanRequestModal.jsx` contains `peminjaman/ajukan`, "Ajukan Peminjaman", and "Ringkasan Peminjaman"
    - `npx eslint` on the three files reports no errors
  </acceptance_criteria>
  <done>Three new components exist, lint-clean, with exact UI-SPEC copy and status/variant maps.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Pinjam entry point on SalinanTable + BukuDetailPage (with D-04 pre-check)</name>
  <files>frontend/src/components/SalinanTable.jsx, frontend/src/pages/BukuDetailPage.jsx</files>
  <read_first>
    - frontend/src/components/SalinanTable.jsx (current 3-column table, kondisiLabel/statusLabel/statusColor maps — extend, do not rewrite the existing display)
    - frontend/src/pages/BukuDetailPage.jsx (role detection lines 17-20, refreshKey pattern lines 25/62, SalinanTable usage line 183, BookFormModal conditional-render lines 202-213, the categories useEffect lines 28-35 as the pattern for fetching pre-check data)
    - frontend/src/components/Toast.jsx (props: message/type/onClose — first real usage)
    - frontend/src/components/BookCard.jsx (check for an existing "Pinjam" button style to reuse; otherwise use the inline-flex pill pattern noted in PATTERNS)
    - frontend/src/lib/api.js, frontend/src/lib/auth.js (getToken/decodeToken → peran)
    - .planning/phases/03-loan-request-approval-workflow/03-01-SUMMARY.md (GET /api/peminjaman mahasiswa response shape — confirm it returns top-level `items`, `total`, and `is_diblokir`; used to derive active-loan count + the blocked flag for the pre-check)
    - .planning/phases/03-loan-request-approval-workflow/03-UI-SPEC.md ("/katalog/:id Pinjam entry point" section; Pinjam button copy/color/icon)
  </read_first>
  <action>
    Modify `SalinanTable.jsx`: add props `{ salinan = [], peran, onPinjam, pinjamDisabled = false }`. When `peran === 'mahasiswa'`, render a 4th `<th>Aksi</th>` column and a matching `<td>` per row. For rows where `s.status_ketersediaan === 'tersedia'`, render a "Pinjam" button: `bg-sage-green text-on-primary hover:bg-sage-green/90`, `bookmark_add` icon, `min-h-[44px]`, `inline-flex items-center gap-1.5 px-4 py-2 rounded-full text-label-sm font-label-sm`, `aria-label={`Pinjam salinan di ${s.lokasi_rak}`}`, `disabled={pinjamDisabled}` + `aria-disabled` + `opacity-50 cursor-not-allowed` when disabled, `onClick={() => onPinjam(s)}`. Non-tersedia rows render `—` in the Aksi cell. Keep the existing 3 columns and label/color maps unchanged for pustakawan (no Aksi column when peran !== 'mahasiswa').
    Modify `BukuDetailPage.jsx`: add state `showLoanModal`, `selectedSalinan`, `toast`, and pre-check state `activeLoanCount`/`isBlocked`. In a useEffect (mahasiswa only, mirroring the categories fetch), call `GET /api/peminjaman` and read the 03-01-SUMMARY mahasiswa response shape: set `activeLoanCount` = count of `items` whose `status_peminjaman` is in {menunggu_persetujuan, siap_diambil, dipinjam}, and set `isBlocked` = the response's top-level `is_diblokir` field (Plan 01 now returns this for the mahasiswa branch — read it directly; do NOT derive a fallback). Compute `blocked = isBlocked`, `atLimit = activeLoanCount >= 5`, `pinjamDisabled = blocked || atLimit`. Render `<BlockedBanner variant={blocked ? 'blocked' : atLimit ? 'limit' : null} />` after the back link, before the book header — so both the `blocked` (LOAN-03, is_diblokir) and `limit` (LOAN-02, count>=5) variants are reachable from real data. Pass `peran`, `pinjamDisabled`, and `onPinjam={(s) => { setSelectedSalinan(s); setShowLoanModal(true); }}` to `<SalinanTable>`. Render `{showLoanModal && <LoanRequestModal buku={buku} salinan={selectedSalinan} mahasiswaNama={decoded?.nama} onClose={() => setShowLoanModal(false)} onSubmitted={() => { setShowLoanModal(false); setToast({ message: 'Pengajuan peminjaman berhasil dikirim. Menunggu persetujuan pustakawan.', type: 'success' }); setRefreshKey((k) => k + 1); }} onError={(msg) => setToast({ message: msg, type: 'error' })} />}` following the existing BookFormModal conditional-render pattern. Add `{toast && <Toast {...toast} onClose={() => setToast(null)} />}` at the page root (first real Toast usage). Bumping refreshKey re-fetches the book so the requested copy flips tersedia→dipesan; re-run the pre-check on refreshKey so the banner/disabled state stays in sync after a successful request.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/components/SalinanTable.jsx src/pages/BukuDetailPage.jsx 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/SalinanTable.jsx` contains `onPinjam` and `bookmark_add` and `min-h-[44px]`
    - `frontend/src/pages/BukuDetailPage.jsx` contains `LoanRequestModal`, `BlockedBanner`, `Toast`, and reads `is_diblokir` from the GET /api/peminjaman response (grep `is_diblokir` returns a hit)
    - `npx eslint` on both files reports no errors (no set-state-in-effect violations)
    - `npm run build` succeeds (exit 0)
  </acceptance_criteria>
  <done>Mahasiswa can open the loan modal from a tersedia copy row; both blocked (is_diblokir) and limit (count>=5) banner variants + disabled buttons appear from real API data; success path re-fetches and toasts.</done>
</task>

<task type="auto">
  <name>Task 3: Create PinjamanPage (mahasiswa branch) + register the /pinjaman route</name>
  <files>frontend/src/pages/PinjamanPage.jsx, frontend/src/router.jsx</files>
  <read_first>
    - frontend/src/pages/KatalogPage.jsx (role detection, refreshKey + useEffect fetch with AbortController, loading/empty/error states, SkeletonGrid pattern — the PinjamanPage analog)
    - frontend/src/components/StatusBadge.jsx (created in Task 1 — used per row)
    - frontend/src/components/BlockedBanner.jsx (created in Task 1 — mahasiswa view banner)
    - frontend/src/components/EmptyState.jsx (empty-state with actionLabel/onAction for "Jelajahi Katalog")
    - frontend/src/components/BookCoverPlaceholder.jsx (small cover in the Buku cell)
    - frontend/src/router.jsx (current `{ path: 'pinjaman', element: <ComingSoonPage title="Riwayat Peminjaman" /> }` line 29 to replace; keep position in the AppShell ProtectedRoute children)
    - frontend/src/lib/api.js, frontend/src/lib/auth.js
    - .planning/phases/03-loan-request-approval-workflow/03-01-SUMMARY.md (GET /api/peminjaman response shape; mahasiswa items list + top-level is_diblokir)
    - .planning/phases/03-loan-request-approval-workflow/03-UI-SPEC.md (Mahasiswa "Pinjaman Saya" view section; Copywriting Contract Tanggal-column-value rows; empty/loading/error copy)
  </read_first>
  <action>
    Create `frontend/src/pages/PinjamanPage.jsx` following KatalogPage's role-detection + refreshKey + AbortController useEffect pattern. Derive `peran` from the decoded token. Fetch `GET /api/peminjaman` on mount and on refreshKey bump. IMPORTANT: this plan implements ONLY the mahasiswa branch — render the pustakawan branch as a placeholder/null guarded by `peran === 'pustakawan'` (Plan 03 fills it in; do not delete the branch). Page shell: `<main class="max-w-container-max mx-auto px-margin-mobile md:px-margin-desktop py-8 md:py-12 space-y-8">`, header `<h1>Pinjaman</h1>` (`headline-md`) + mahasiswa subtitle "Riwayat dan status pengajuan peminjaman Anda."
    Mahasiswa branch: render `<BlockedBanner variant={blocked ? 'blocked' : atLimit ? 'limit' : null} />` below the header, deriving `blocked` from the response's top-level `is_diblokir` field and `atLimit` from the active-loan count of `items` (same data the Plan 02 pre-check reads — no fallback derivation). A single `<section>` card with header "Pinjaman Saya" (`headline-sm`) and a table with columns "Buku" | "Status" | "Tanggal". Sort items by tanggal_pengajuan descending. Per row: Buku cell = small BookCoverPlaceholder + judul + penulis; Status cell = `<StatusBadge status={row.status_peminjaman} />` (all 5 statuses, D-10); Tanggal cell = contextual label per UI-SPEC: menunggu_persetujuan → "Diajukan {tanggal_pengajuan}", siap_diambil → "Ambil sebelum {tanggal_siap_ambil + 2 hari, formatted 'D MMM YYYY, HH:mm'}" (D-11, absolute), dipinjam → "Tenggat {tanggal_tenggat}", ditolak/dibatalkan → "Diajukan {tanggal_pengajuan}". Use `toLocaleDateString`/`toLocaleString` with `id-ID` locale; add 2 days to tanggal_siap_ambil via Date arithmetic for the pickup-deadline label. Loading → skeleton rows (animate-pulse). Error → EmptyState `error` icon + "Gagal memuat data pinjaman. Periksa koneksi Anda dan coba lagi." + "Coba Lagi" retrying via refreshKey. Empty (no loans) → EmptyState icon `local_library`, heading "Belum Ada Pinjaman", body "Anda belum pernah mengajukan peminjaman. Jelajahi katalog untuk meminjam buku.", action "Jelajahi Katalog" navigating to /katalog.
    Modify `router.jsx`: import `PinjamanPage from './pages/PinjamanPage'`; replace the `pinjaman` ComingSoonPage element with `<PinjamanPage />`, keeping its position in the AppShell-wrapped ProtectedRoute children. Leave dashboard/anggota ComingSoonPage entries unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/pages/PinjamanPage.jsx src/router.jsx 2>&1 | tail -10 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/PinjamanPage.jsx` contains "Pinjaman Saya", `StatusBadge`, and `api.get('/peminjaman'`
    - `frontend/src/router.jsx` contains `<PinjamanPage` and no longer maps `pinjaman` to ComingSoonPage
    - `npx eslint` on both files reports no errors
    - `npm run build` succeeds (exit 0)
  </acceptance_criteria>
  <done>/pinjaman renders the mahasiswa "Pinjaman Saya" list with 5-status badges and contextual dates; pustakawan branch is a guarded placeholder for Plan 03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify mahasiswa loan-request flow + Pinjaman Saya list</name>
  <action>Pause for the user to manually verify the mahasiswa loan-request slice in a running app, per the how-to-verify steps below. No code changes — this is a human verification gate.</action>
  <what-built>Mahasiswa loan-request slice: Pinjam button on tersedia copy rows, LoanRequestModal confirmation, BlockedBanner pre-check (both limit and blocked variants), success/error Toasts, and the "Pinjaman Saya" list at /pinjaman with status badges and contextual dates.</what-built>
  <how-to-verify>
    1. Run `docker-compose up` (or the frontend+backend dev servers) and log in as a seeded mahasiswa.
    2. Visit a book detail page `/katalog/{id}` that has a tersedia copy. Confirm each tersedia copy row shows a green "Pinjam" button; non-tersedia rows show "—".
    3. Click "Pinjam" → the LoanRequestModal opens showing Buku Terpilih (title + Lokasi Rak), Informasi Peminjam (your name + Mahasiswa), and Ringkasan Peminjaman with an "Estimasi Tenggat Waktu" date ~14 days out. Click "Ajukan Peminjaman" → success toast appears, modal closes, and the copy's status changes from Tersedia to Dipesan.
    4. Visit /pinjaman → confirm "Pinjaman Saya" lists your new request with a "Menunggu Persetujuan" badge and a "Diajukan ..." date, sorted newest-first.
    5. (LOAN-03) Seed (or flip in the DB) an is_diblokir=TRUE mahasiswa, log in as them, and confirm the red "Akun Diblokir" BlockedBanner shows at the top of /katalog/{id} and /pinjaman with all Pinjam buttons disabled (this is now driven by the GET /api/peminjaman top-level is_diblokir field).
    6. (LOAN-02) Seed a mahasiswa with 5 active loans and confirm the gold "Batas Pinjaman Tercapai" BlockedBanner shows and all Pinjam buttons are disabled. Also confirm that even if you bypass the UI, the API returns 400 (covered by Plan 01 tests).
    7. Resize the browser to 375px and confirm the modal, banner, and table remain usable (horizontal scroll on the table is acceptable).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (mahasiswa) → API | The UI pre-check (D-04 disable + banner) is UX only; the API is the authoritative gate. A user can re-enable buttons via devtools — the server still enforces LOAN-02/03. |
| rendered API data → DOM | Loan/book strings (judul, nama) rendered into JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | client-side D-04 pre-check bypass | mitigate | The disabled "Pinjam" buttons are UX only; the real gate is Plan 01's server-side LOAN-02/03 checks (400). LoanRequestModal maps the 400 detail to a specific error Toast and keeps the modal open — no client-trusted authorization. The is_diblokir flag the banner reads is server-supplied, not client-derived. |
| T-03-09 | Spoofing | role detection from JWT in browser | accept | `peran` decoded from the JWT client-side selects which UI branch renders; this is presentation only. All data access is re-authorized server-side per Plan 01 (T-03-01/02/07). A tampered client token fails server JWT validation (401). |
| T-03-10 | Information Disclosure | XSS via rendered loan/book strings | mitigate | React escapes string children by default; no `dangerouslySetInnerHTML` is introduced. StatusBadge/BlockedBanner render fixed Indonesian copy, not user input. |
| T-03-SC | Tampering | npm installs | accept | No new npm packages — all new components are hand-built React + Tailwind reusing existing deps (Material Symbols already loaded). No package-manager installs in this plan. |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds with all new/modified files
- `npx eslint` clean on StatusBadge, BlockedBanner, LoanRequestModal, SalinanTable, BukuDetailPage, PinjamanPage, router
- BlockedBanner `blocked` variant is driven by the GET /api/peminjaman top-level is_diblokir field (LOAN-03) and the `limit` variant by active-loan count >= 5 (LOAN-02) — both reachable from real data
- Human checkpoint confirms the request flow, both banner variants/disable behavior, and "Pinjaman Saya" list render correctly down to 375px
</verification>

<success_criteria>
- A mahasiswa can request a specific tersedia copy from /katalog/{id} and see it become dipesan (LOAN-01)
- At-limit mahasiswa see the gold "limit" banner + disabled buttons (LOAN-02); is_diblokir mahasiswa see the red "blocked" banner + disabled buttons (LOAN-03), driven by the server's is_diblokir flag; API rejects bypass attempts with 400
- /pinjaman shows the mahasiswa's loans with correct 5-status badges and contextual dates (D-09/D-10/D-11)
</success_criteria>

<output>
Create `.planning/phases/03-loan-request-approval-workflow/03-02-SUMMARY.md` when done. Note the StatusBadge/BlockedBanner/LoanRequestModal props and confirm that the mahasiswa GET response's top-level is_diblokir field drives the blocked banner (so Plan 03 and the pustakawan branch know the data shape).
</output>
</output>
