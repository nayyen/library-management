---
phase: 04-returns-fines-blocking
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/tests/test_peminjaman.py
  - backend/app/schemas/peminjaman.py
  - backend/app/routers/peminjaman.py
  - frontend/src/pages/PinjamanPage.jsx
autonomous: true
requirements: [RET-04]
must_haves:
  truths:
    - "A pustakawan sees an Anggota Diblokir section listing every is_diblokir=true mahasiswa with their total denda owed (D-04, D-06)"
    - "Denda owed per blocked member equals SUM(total_denda) across that member's dikembalikan loans (D-06)"
    - "A pustakawan can click Denda Lunas to clear is_diblokir on the member's account, leaving total_denda rows unchanged as historical record (RET-04, D-05)"
  artifacts:
    - path: "backend/app/routers/peminjaman.py"
      provides: "PUT /api/peminjaman/anggota/{id_pengguna}/lunasi_denda endpoint, anggota_diblokir list in pustakawan GET branch"
      contains: "def lunasi_denda"
    - path: "backend/app/schemas/peminjaman.py"
      provides: "AnggotaDiblokirOut shape; PeminjamanResponse extended with anggota_diblokir"
      contains: "class AnggotaDiblokirOut"
    - path: "frontend/src/pages/PinjamanPage.jsx"
      provides: "Anggota Diblokir card-list section + handleLunasiDenda mutation handler"
      contains: "handleLunasiDenda"
    - path: "backend/tests/test_peminjaman.py"
      provides: "RED tests for lunasi_denda clears block, lunasi_denda forbidden for mahasiswa, lunasi_denda 404, anggota_diblokir list with summed denda"
      contains: "def test_lunasi_denda"
  key_links:
    - from: "frontend/src/pages/PinjamanPage.jsx"
      to: "PUT /api/peminjaman/anggota/{id_pengguna}/lunasi_denda"
      via: "api.put in handleLunasiDenda"
      pattern: "lunasi_denda"
    - from: "backend/app/routers/peminjaman.py lunasi_denda"
      to: "pengguna.is_diblokir = False"
      via: "unblock mutation"
      pattern: "is_diblokir = False"
    - from: "backend/app/routers/peminjaman.py anggota_diblokir"
      to: "func.sum(Peminjaman.total_denda)"
      via: "per-member denda aggregate over dikembalikan loans"
      pattern: "func.sum"
---

## Phase Goal

**As a** pustakawan, **I want to** see which members are blocked over unpaid fines and mark a fine as paid, **so that** I can unblock a member's account once they have settled their denda at the desk.

<objective>
Deliver the unblock vertical slice end-to-end: a pustakawan opens the new "Anggota Diblokir" section on `/pinjaman`, sees a card per blocked mahasiswa with their summed outstanding denda, clicks "Denda Lunas", confirms, and the backend clears `is_diblokir` (leaving the historical `total_denda` rows untouched), so the member can borrow again.

After Task 3 a pustakawan can actually unblock a real blocked member through the UI — a capability that did not exist after Plan 01.

Purpose: Implements RET-04 (mark fine paid → clear block). Implements locked decisions D-04, D-05, D-06.
Output: `lunasi_denda` endpoint, `AnggotaDiblokirOut` schema + `anggota_diblokir` list, new "Anggota Diblokir" card-list section, RED→GREEN backend tests.

Depends on Plan 01: extends the same three files (`peminjaman.py` router + schema, `PinjamanPage.jsx`) after the return slice is in place. Runs in Wave 2 because of that file overlap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-returns-fines-blocking/04-CONTEXT.md
@.planning/phases/04-returns-fines-blocking/04-PATTERNS.md
@.planning/phases/04-returns-fines-blocking/04-UI-SPEC.md
@.planning/phases/04-returns-fines-blocking/04-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols newly created by Phase 4, Plan 02 (exclude from upstream-drift verification):

- Backend: `lunasi_denda` endpoint function (path `PUT /api/peminjaman/anggota/{id_pengguna}/lunasi_denda`), `anggota_diblokir` local list + `blocked_users` query in the pustakawan GET branch, `func` import from `sqlalchemy`.
- Schema: `AnggotaDiblokirOut` class; `PeminjamanResponse` field `anggota_diblokir`.
- Frontend: `handleLunasiDenda` function, "Anggota Diblokir" `<section>` (card list), `anggotaDiblokir` derived const.
- Tests: `test_lunasi_denda_*` functions and the `anggota_diblokir` list test.

(Plan 01 produced `kembalikan`, `_is_terlambat`, `sedang_dipinjam`, the `terlambat` badge. Plan 03 adds the mahasiswa-side `denda_tertunggak`, Denda column, and BlockedBanner amount.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing backend tests for lunasi_denda unblock and anggota_diblokir list</name>
  <files>backend/tests/test_peminjaman.py</files>
  <read_first>
    - backend/tests/test_peminjaman.py (REQUIRED — read the whole file including the `_seed_dipinjam_peminjaman` helper added in Plan 01; reuse `_create_pustakawan_token`, `_register_and_login`, `_auth_header`, and the kembalikan flow to produce a blocked member)
    - backend/tests/conftest.py (REQUIRED — `client`/`db_session` fixtures)
    - backend/app/models/pengguna.py (REQUIRED — `is_diblokir` column)
    - backend/app/models/peminjaman.py (REQUIRED — `total_denda`, `status_peminjaman`, `id_pengguna` columns; `dikembalikan` aggregate scope)
    - .planning/phases/04-returns-fines-blocking/04-01-SUMMARY.md (REQUIRED — confirms the kembalikan endpoint shape used to set up a blocked member)
  </read_first>
  <behavior>
    - test_lunasi_denda_clears_block: arrange a mahasiswa with is_diblokir=True (set directly, or via a late kembalikan); pustakawan PUTs /api/peminjaman/anggota/{id_pengguna}/lunasi_denda → 200; pengguna.is_diblokir == False after; existing peminjaman.total_denda values on dikembalikan rows are UNCHANGED (D-05).
    - test_lunasi_denda_forbidden_for_mahasiswa: mahasiswa token → 403.
    - test_lunasi_denda_404: random/unknown id_pengguna → 404.
    - test_list_pustakawan_anggota_diblokir: seed one blocked mahasiswa with two dikembalikan loans (total_denda 16000 and 4000); pustakawan GET /api/peminjaman → `anggota_diblokir` list contains one entry with id_pengguna, nama, email, and total_denda == 20000 (SUM across dikembalikan loans, D-06). A non-blocked member does NOT appear.
  </behavior>
  <action>
    Add the four test functions named above. To arrange a blocked member, prefer driving the Plan 01 kembalikan endpoint on an overdue `_seed_dipinjam_peminjaman` loan (most realistic), or set `user.is_diblokir = True` and seed `Peminjaman` rows with `status_peminjaman=StatusPeminjaman.dikembalikan` + explicit `total_denda` values directly via `db_session` for the SUM test. Endpoint path is `PUT /api/peminjaman/anggota/{id_pengguna}/lunasi_denda` (pustakawan-only, path-param only, no request body). Assert the response is 200 and re-query `Pengguna` to confirm `is_diblokir` flipped to False while `total_denda` rows are untouched. Tests MUST fail now (endpoint + anggota_diblokir field do not exist) — RED step, do NOT implement.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_peminjaman.py -k lunasi_denda -x 2>&1 | grep -q "failed\|error" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_peminjaman.py` contains `def test_lunasi_denda_clears_block`, `def test_lunasi_denda_forbidden_for_mahasiswa`, `def test_lunasi_denda_404`, `def test_list_pustakawan_anggota_diblokir`
    - The SUM test asserts `total_denda == 20000` for a member with two dikembalikan loans of 16000 + 4000
    - The clears_block test re-queries `Pengguna` and asserts `is_diblokir` is False while the dikembalikan `total_denda` rows are unchanged
    - `cd backend && python -m pytest tests/test_peminjaman.py -k lunasi_denda` exits non-zero (RED)
  </acceptance_criteria>
  <done>The four tests exist and FAIL because the `lunasi_denda` endpoint and `anggota_diblokir` field do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — AnggotaDiblokirOut schema, lunasi_denda endpoint, anggota_diblokir aggregate list</name>
  <files>backend/app/schemas/peminjaman.py, backend/app/routers/peminjaman.py</files>
  <read_first>
    - backend/app/routers/peminjaman.py (REQUIRED — read the whole file including Plan 01 additions; reuse `_pustakawan_only`, the joinedload+404 lookup shape from `serahkan_peminjaman`/`kembalikan`, and the pustakawan GET branch where `sedang_dipinjam` was added)
    - backend/app/schemas/peminjaman.py (REQUIRED — `PeminjamanResponse` with its Plan 01 `sedang_dipinjam` field; add the new field alongside)
    - .planning/phases/04-returns-fines-blocking/04-PATTERNS.md (REQUIRED — `AnggotaDiblokirOut` schema lines 291-300, anggota_diblokir aggregate query lines 213-239, `from sqlalchemy import func`)
    - backend/app/models/pengguna.py (REQUIRED — `Pengguna.is_diblokir`, `Pengguna.nama`, `Pengguna.email`, `Pengguna.id`)
    - backend/app/models/enums.py (REQUIRED — `StatusPeminjaman.dikembalikan`)
  </read_first>
  <action>
    In `backend/app/schemas/peminjaman.py`: add `class AnggotaDiblokirOut(BaseModel)` with `id_pengguna: str`, `nama: str`, `email: str`, `total_denda: int`, and `model_config = {"from_attributes": True}`. Extend `PeminjamanResponse` with `anggota_diblokir: list[AnggotaDiblokirOut] | None = None`.
    In `backend/app/routers/peminjaman.py`: add `from sqlalchemy import func` to imports and `AnggotaDiblokirOut` to the `from app.schemas.peminjaman import (...)` block. Add endpoint `@router.put("/anggota/{id_pengguna}/lunasi_denda")` (pustakawan-only): call `_pustakawan_only(user)`; query `Pengguna` by id; 404 (detail "Anggota tidak ditemukan.") if missing; set `pengguna.is_diblokir = False` (do NOT touch any `total_denda` per D-05); `db.commit()`; return a typed ack (reuse `AnggotaDiblokirOut` recomputing the SUM, or a minimal dict — keep `response_model` typed consistent with other endpoints). In the `daftar_peminjaman` pustakawan branch, build `anggota_diblokir`: query `Pengguna` where `is_diblokir == True`, and for each compute `db.query(func.sum(Peminjaman.total_denda)).filter(id_pengguna == u.id, status_peminjaman == StatusPeminjaman.dikembalikan).scalar() or 0`, appending `AnggotaDiblokirOut(id_pengguna=str(u.id), nama=u.nama, email=u.email, total_denda=total)`. Include `anggota_diblokir` in the returned `PeminjamanResponse` alongside `sedang_dipinjam`. Implements RET-04, D-04, D-05, D-06. GREEN step — make Task 1 tests pass without weakening them; do not regress Plan 01 tests.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_peminjaman.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_peminjaman.py` exits 0 (Plan 01 + Plan 02 tests all GREEN)
    - `backend/app/schemas/peminjaman.py` contains `class AnggotaDiblokirOut` and `anggota_diblokir:`
    - `backend/app/routers/peminjaman.py` contains `def lunasi_denda`, `is_diblokir = False`, `func.sum`, and route `"/anggota/{id_pengguna}/lunasi_denda"`
    - The SUM test (`total_denda == 20000`) and the unblock test (`is_diblokir` False, denda rows unchanged) pass
  </acceptance_criteria>
  <done>The lunasi_denda endpoint clears the block without altering historical denda, and the pustakawan list exposes anggota_diblokir with per-member summed denda over dikembalikan loans.</done>
</task>

<task type="auto">
  <name>Task 3: UI — Anggota Diblokir card-list section wired to lunasi_denda</name>
  <files>frontend/src/pages/PinjamanPage.jsx</files>
  <read_first>
    - frontend/src/pages/PinjamanPage.jsx (REQUIRED — read the whole file including the Plan 01 "Sedang Dipinjam" section and `handleKembalikan`; copy the mutation cycle and the `<section>` shell; reuse `showConfirm`/`setConfirmLoading`/`setRefreshKey`/`setToast`/`setConfirm` and `EmptyState`)
    - .planning/phases/04-returns-fines-blocking/04-UI-SPEC.md (REQUIRED — Anggota Diblokir section spec lines 227-283: exact per-card markup, the "Denda Lunas" button markup with `lock_open` icon + `bg-sage-green`, ConfirmDialog copy, empty-state copy, and the Open Question note confirming ConfirmDialog stays two-tone — do NOT add a sage-green confirm variant)
    - .planning/phases/04-returns-fines-blocking/04-PATTERNS.md (REQUIRED — `handleLunasiDenda` reference lines 487-507, Anggota Diblokir section assembly line 516)
    - frontend/src/components/EmptyState.jsx (REQUIRED — confirm prop shape `icon`/`title`/`message`)
  </read_first>
  <action>
    In `PinjamanPage.jsx`: add `handleLunasiDenda(item)` mirroring `handleKembalikan` — `showConfirm` title "Tandai Denda Lunas?", message `Denda sebesar Rp ${item.total_denda.toLocaleString('id-ID')} milik ${item.nama} akan dinyatakan lunas dan akun akan dibuka kembali.`, confirmLabel "Denda Lunas", `destructive: false`; onConfirm `api.put(\`/peminjaman/anggota/${item.id_pengguna}/lunasi_denda\`)`, `setConfirm(null)`, success toast `Denda dinyatakan lunas. Akun ${item.nama} tidak lagi diblokir.`, `setRefreshKey((k)=>k+1)`; catch → error toast "Gagal memproses tindakan. Silakan coba lagi." Add `const anggotaDiblokir = loanData?.anggota_diblokir ?? [];`. Render a new "Anggota Diblokir" `<section>` as the 4th/last pustakawan section (after "Sedang Dipinjam", inside the `!isMahasiswa && !loading` block) using the same `<section>` shell + header (title "Anggota Diblokir", subtitle "Anggota dengan denda tertunggak — buka blokir setelah denda dibayar."), but a card-list body `<div className="p-6 space-y-4">` rendering one card per item using the exact card markup from UI-SPEC lines 240-267 (avatar initial from `item.nama`, name, email, "Denda Tertunggak" label + `Rp {item.total_denda.toLocaleString('id-ID')}` in crimson, and the "Denda Lunas" button calling `handleLunasiDenda(item)`). Empty state: `<EmptyState icon="task_alt" title="Tidak Ada Anggota Diblokir" message="Semua anggota dalam status baik — tidak ada denda tertunggak." />`. Implements D-04, D-06. Do NOT modify ConfirmDialog.jsx (UI-SPEC Open Question: antique-gold fallback is acceptable).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/PinjamanPage.jsx` contains `function handleLunasiDenda`, `/peminjaman/anggota/${item.id_pengguna}/lunasi_denda`, `Anggota Diblokir`, `anggota_diblokir`, `lock_open`, and `bg-sage-green`
    - `frontend/src/pages/PinjamanPage.jsx` empty state contains `Tidak Ada Anggota Diblokir`
    - `frontend/src/components/ConfirmDialog.jsx` is NOT in the git diff for this task (unchanged)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Pustakawan sees a card per blocked member with summed outstanding denda; clicking Denda Lunas confirms and unblocks the account, refreshing the section and showing the success toast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → FastAPI (`/api/peminjaman/anggota/{id}/lunasi_denda`) | Untrusted JWT request; pustakawan role required to clear another account's block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Elevation of Privilege | `PUT /lunasi_denda` | mitigate | `_pustakawan_only(user)` first statement; covered by `test_lunasi_denda_forbidden_for_mahasiswa` asserting 403. A mahasiswa must never be able to self-unblock |
| T-04-07 | Tampering | unblocking a non-existent member | mitigate | 404 on unknown `id_pengguna` before any mutation; covered by `test_lunasi_denda_404` |
| T-04-08 | Repudiation / integrity | clearing block must not erase fine history | mitigate | Endpoint sets only `is_diblokir = False`; `total_denda` rows are never touched (D-05); `test_lunasi_denda_clears_block` asserts denda rows unchanged |
| T-04-09 | Information Disclosure | anggota_diblokir exposes member email + denda | accept | Returned only on the pustakawan GET branch (role-gated); pustakawan already manage all members. Mahasiswa branch never includes this list |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages — `func` is from the already-installed `sqlalchemy`; no Package Legitimacy Gate needed |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_peminjaman.py` exits 0 (Plan 01 + Plan 02 GREEN, no regressions)
- `cd frontend && npm run build` exits 0
- Unblock path: a blocked member's `is_diblokir` flips to False; their dikembalikan `total_denda` rows remain unchanged
- anggota_diblokir aggregate: two dikembalikan loans (16000 + 4000) → member shows `total_denda == 20000`
</verification>

<success_criteria>
- RET-04: pustakawan marks a fine paid via "Denda Lunas" → `is_diblokir` cleared on the member's account
- D-04: "Anggota Diblokir" section lists is_diblokir=true members as cards with summed denda
- D-05: clearing the block leaves historical `total_denda` rows untouched
- D-06: per-member denda = SUM(total_denda) over dikembalikan loans
</success_criteria>

<output>
Create `.planning/phases/04-returns-fines-blocking/04-02-SUMMARY.md` when done
</output>
